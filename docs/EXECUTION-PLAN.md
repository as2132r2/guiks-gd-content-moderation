# 团队分工与截止时间

提交截止：2026-08-29 24:00。目标是在 8 月 29 日 20:00 代码冻结，保留四小时用于彩排、录屏和提交故障。

## 负责人

### 黄博文（`huangbow`）— Gateway 与审核内核，技术把关

- 把现有 Gateway 改成真实可用的 Anthropic 兼容模型出口。
- 接入 GLM，保留 DeepSeek 配置位。
- 改造入口准入和输出预检 detector。
- 实现一致性比对与结构化审核结果。
- 审核共享契约、模型出口和安全相关 PR。

主要目录：`src/model/`、`src/lib/detectors*`、`src/routes/gateway*`。

### William Tu（`William-Th`）— 生产链路与演示界面

- 通稿生成播报稿和短视频文案。
- 稿件工作台、预检标注和角色切换。
- 三审流转和 AI 参与度追溯主屏。
- 保证三分钟演示路径连贯。

主要目录：`src/views/`、工作台与稿件相关 routes。

### Leo（`as2132r2`）— 底座、集成与发布

- 仓库、共享契约、SQLite/Drizzle、SSE 和配置。
- Docker、本地启动、环境变量、健康检查。
- 每两小时集成主分支，保证 `main` 始终可演示。
- 维护 README、架构决策、提交清单和最终部署。

主要目录：`src/domain/`、`src/db/`、配置、CI、Docker、docs。

### 刘浩（`Coder-sonson`）— 规则数据、测试与演示素材

- 整理首批确定性禁用/慎用词规则和测试 fixture。
- 准备脱敏通稿、正常/要理由/硬拦/公器私用四组样例。
- 为数字、日期、人名、职务不一致编写测试。
- 跑主链回归、记录问题、协助作品说明和演示口播。

主要目录：`src/rules/`、`test/fixtures/`、`test/`、`docs/demo/`。

## 时间表

| 时间 | 必须达到的状态 |
| --- | --- |
| 8/28 17:00 | 仓库、契约、分支、Mock 六步主链确定 |
| 8/28 20:00 | 页面能从素材走到追溯；允许全是 Mock，但路径不能断 |
| 8/28 23:30 | 真实模型生成接通；至少两类 L1 预检可复现 |
| 8/29 10:00 | 入口准入、生成、预检、三审、追溯全部联通 |
| 8/29 15:00 | 功能冻结；补对照组、异常提示和演示数据 |
| 8/29 18:00 | 连续完整彩排三遍；单次控制在三分钟 |
| 8/29 20:00 | 代码冻结；只修阻塞演示的问题 |
| 8/29 22:00 | Demo、作品说明、录屏和提交包全部就绪 |
| 8/29 24:00 | 官方截止；最后两小时只作缓冲 |

## 集成纪律

1. 第一优先级永远是六步主链可运行，不做主链之外的“顺手优化”。
2. `main` 必须随时能启动和演示；每个 PR 尽量控制在一个可验证目标。
3. 共享接口先写类型和示例响应，再各自开发。
4. 18:00 后不换框架、不换数据库、不重做 UI 技术栈。
5. L2 结果一律使用“待人工复核”，不输出自动终审结论。
6. 素材不是来自真实机构时，演示和作品说明必须明确写“模拟/脱敏素材”。
